[package]
name = "motrworks"
version = "0.1.0"
description = "MotrWorks — CortexEdge electric-machine design tools (desktop shell)"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
strip = true
lto = true
codegen-units = 1
